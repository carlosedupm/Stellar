package br.com.stellar.api.controller.api.assembler;

import org.springframework.stereotype.Component;

@Component
public class ProdutoModelAssembler {
	/*
	 * @Autowired private ModelMapper modelMapper;
	 * 
	 * public ProdutoModel toModel(Produto produto) { return
	 * modelMapper.map(produto, ProdutoModel.class); }
	 * 
	 * public List<ProdutoModel> toCollectionModel(List<Produto> produtos) { return
	 * produtos.stream() .map(produto -> toModel(produto))
	 * .collect(Collectors.toList()); }
	 */

}
